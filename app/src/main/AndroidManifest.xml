<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rks369.foodrunner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Food Runner"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.rks369.foodrunner.activity.CartActivity"></activity>
        <activity android:name="com.rks369.foodrunner.activity.ResetPasswordActivity" />
        <activity android:name="com.rks369.foodrunner.activity.RestaurantMenuActivity" />
        <activity android:name="com.rks369.foodrunner.activity.DashboardActivity" />
        <activity android:name="com.rks369.foodrunner.activity.ForgotPasswordActivity" />
        <activity android:name="com.rks369.foodrunner.activity.RegisterActivity" />
        <activity android:name="com.rks369.foodrunner.activity.LoginActivity" />
        <activity android:name="com.rks369.foodrunner.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>